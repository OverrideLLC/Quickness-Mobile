package org.quickness.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de colores de la aplicación Quickness

/**
 * Color de fondo principal de la aplicación.
 * Este color se utiliza como fondo predominante de la interfaz.
 */
val Background = Color(0xFF1b1b1b) // Background

/**
 * Color secundario.
 * Se usa para áreas secundarias de la interfaz o elementos de fondo complementarios.
 */
val Secondary = Color(0xFF000000) // Secondary

/**
 * Color para elementos que aparecen sobre el fondo principal.
 * Adecuado para texto u otros componentes que necesitan contraste con el fondo.
 */
val OnBackground = Color(0xFF292929) // OnBackground

/**
 * Color primario de la aplicación.
 * Usado para elementos destacados como botones principales o enlaces.
 */
val Primary = Color(0xFF5ce1e6) // Primary

/**
 * Color para elementos que aparecen sobre el color secundario.
 * Ideal para texto o íconos sobre áreas secundarias de la interfaz.
 */
val OnSecondary = Color(0xFFfefefe) // OnSecondary

/**
 * Color de error.
 * Se utiliza para indicar errores o advertencias en la interfaz.
 */
val Error = Color(0xFFff3131) // Error

val tertiary = Color(0xFFFFFFFF)

val Success = Color(0xFF00FF00)